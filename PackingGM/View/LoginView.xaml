<Page x:Class="PackingGM.View.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PackingGM.View"
      xmlns:model="clr-namespace:PackingGM.Model"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginView">
    <Grid Style="{DynamicResource GridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="16"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Style="{DynamicResource TextBlockHeadStyle}"
                   FontSize="30"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="Вход"/>
        <StackPanel Grid.Row="1"
                    Width="300">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left"
                           Style="{DynamicResource TextBlockStyle}"
                           Text="Логин"/>
                <TextBox DockPanel.Dock="Right"
                         Text="{Binding LogUser.Login}"
                         Width="230"
                         HorizontalAlignment="Right"/>
            </DockPanel>
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" 
                           Style="{DynamicResource TextBlockStyle}"
                           Text="Пароль"/>
                <TextBox DockPanel.Dock="Right"
                         Text="{Binding LogUser.Password}"
                         Width="230"
                         HorizontalAlignment="Right"/>
            </DockPanel>
            <Button Command="{Binding EnterCommand}"
                    Style="{DynamicResource ButtonYesStyle}"
                    Content="Войти"
                    IsEnabled="{Binding CanLogin}"/>
        </StackPanel>
    </Grid>
</Page>
