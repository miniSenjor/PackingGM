<Page x:Class="PackingGM.View.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PackingGM.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView"
      Style="{DynamicResource PageStyle}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Главное окно" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Width="250">
            <Button Command="{Binding OpenChangeRoleCommand}">
                Управление ролями
            </Button>
            <Button Command="{Binding OpenChangeUserCommand}">
                Управление пользователями
            </Button>
            <Button Command="{Binding OpenChangeGraphCommand}">
                Управление графиком
            </Button>
            <Button Command="{Binding OpenTestCommand}">
                Тест
            </Button>
        </StackPanel>
    </Grid>
</Page>
