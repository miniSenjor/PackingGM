<Page x:Class="PackingGM.View.ManageGraphD3View"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PackingGM.View"      
      xmlns:local1="clr-namespace:PackingGM.View.Control"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="1920"
      Title="ManageGraphD3View"
      Style="{DynamicResource PageStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                Command="{Binding BackCommand}"
                Style="{DynamicResource ButtonYesStyle}"
                Content="Назад"
                Width="50"
                HorizontalAlignment="Left"/>
        <ListView ItemsSource="{Binding ListGMs}" SelectedItem="{Binding SelectedGM}" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="40">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ п/п" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Number}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для AggregateType-->
                    <GridViewColumn Width="95">
                        <GridViewColumn.Header>
                            <TextBlock Text="Тип изделия" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding AggregateTypes}"
                                                     SelectedItem="{Binding SelectedAggregateType, Mode=TwoWay}"
                                                     DisplayMemberPath="Name"
                                                     MinWidth="50"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Aggregate -->
                    <GridViewColumn Width="120">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ агрегата" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding Aggregates}"
                                                     SelectedItem="{Binding SelectedAggregate, Mode=TwoWay}"
                                                     DisplayMemberPath="Number"
                                                     Width="Auto"
                                                     MinWidth="60"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                
                    <!-- Колонка для Order -->
                    <GridViewColumn Width="90">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ заказа" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <local1:ComboTextBox ItemsSource="{Binding Orders}"
                                                     SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                                                     DisplayMemberPath="Number"
                                                     Width="Auto"
                                                     MinWidth="50"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для D3 -->
                    <GridViewColumn Width="120">
                        <GridViewColumn.Header>
                            <TextBlock Text="Д3" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding D3s}"
                                                    SelectedItem="{Binding SelectedD3, Mode=TwoWay}"
                                                    DisplayMemberPath="Note"
                                                    Width="Auto"
                                                    MinWidth="50"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для ПР -->
                    <GridViewColumn Width="40">
                        <GridViewColumn.Header>
                            <TextBlock Text="ПР" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.PR}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                
                    <!-- Колонка для GM -->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ ГМ" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding GMNumbers}"
                                                     SelectedItem="{Binding SelectedGMNumber}"
                                                     DisplayMemberPath="NumberGM"
                                                     Width="Auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для SPU -->
                    <GridViewColumn Width="130">
                        <GridViewColumn.Header>
                            <TextBlock Text="Обозначение СПУ" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding SPUs}"
                                                     SelectedItem="{Binding SelectedSPU}"
                                                     DisplayMemberPath="Note"
                                                     Width="Auto"
                                                     MinWidth="50"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Колонка для SPU -->
                    <GridViewColumn Width="170">
                        <GridViewColumn.Header>
                            <TextBlock Text="Наименование СПУ" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding SelectedSPU.Name}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для  Manufactory-->
                    <GridViewColumn Width="55">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ Пр-ва" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding Manufactories}"
                                                     SelectedItem="{Binding SelectedManufactory}"
                                                     DisplayMemberPath="Number"
                                                     Width="Auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                
                    <!-- Колонка для GMDate1 -->
                    <GridViewColumn Width="95">
                        <GridViewColumn.Header>
                            <TextBlock Text="Плановый срок сдачи ГМ" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.Date1, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Колонка для GMDate1 -->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="План-нед" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.Date1.Week, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для GMDNackaldania -->
                    <GridViewColumn Width="70">
                        <GridViewColumn.Header>
                            <TextBlock Text="Накладная" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.NackaldaiaNumber, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для NackadnaiaDate-->
                    <GridViewColumn Width="70">
                        <GridViewColumn.Header>
                            <TextBlock Text="Дата накладной" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.DemindData, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для FactWeek-->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="Факт_нед" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.DemindData, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Причины задаржки сдачи ГМ-->
                    <GridViewColumn Width="120">
                        <GridViewColumn.Header>
                            <TextBlock Text="Причины задаржки сдачи ГМ" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding GM.Await, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Contragent-->
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <TextBlock Text="Контрагент" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <local1:ComboTextBox ItemsSource="{Binding Contragents}"
                                                     SelectedItem="{Binding SelectedContragent}"
                                                     DisplayMemberPath="Name"
                                                     Width="Auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Style="{DynamicResource ButtonYesStyle}"
                    Command="{Binding AddGMCommand}"
                    Grid.Column="0"
                    Content="Добавить"/>
            <Button Style="{DynamicResource ButtonNoStyle}"
                    Command="{Binding DeleteGMCommand}"
                    Grid.Column="1"
                    Width="Auto"
                    Content="Удалить"/>
            <Button Style="{DynamicResource ButtonYesStyle}"
                    Command="{Binding SaveCommand}"
                    Grid.Column="2"
                    Width="Auto"
                    Content="Сохранить"/>
        </Grid>

    </Grid>
</Page>
