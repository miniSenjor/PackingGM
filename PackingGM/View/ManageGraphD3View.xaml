<Page x:Class="PackingGM.View.ManageGraphD3View"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PackingGM.View"      
      xmlns:local1="clr-namespace:PackingGM.View.Control"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="1920"
      Title="ManageGraphD3View">
    <Grid Style="{DynamicResource GridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Command="{Binding BackCommand}"
                    Style="{DynamicResource ButtonYesStyle}"
                    Content="Назад"
                    Width="50"
                    HorizontalAlignment="Left"/>
            <TextBlock Text="График упаковки ГМ Д3"
                       Style="{DynamicResource TextBlockHeadStyle}"
                       FontSize="20"/>
        </StackPanel>
        <ListView ItemsSource="{Binding listGMs}"
                  SelectedItem="{Binding SelectedGM}"
                  IsSynchronizedWithCurrentItem="True"
                  Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="40">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ п/п" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Number}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для AggregateType-->
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <TextBlock Text="Тип изделия" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding AggregateTypes}"
                                                         SelectedItem="{Binding SelectedAggregateType}"
                                                         DisplayMemberPath="Name"
                                                         MinWidth="50"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedAggregateType}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Aggregate -->
                    <GridViewColumn Width="145">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ агрегата" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding Aggregates}"
                                                         SelectedItem="{Binding SelectedAggregate}"
                                                         DisplayMemberPath="Number"
                                                         Width="Auto"
                                                         MinWidth="60"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedAggregate}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Order -->
                    <GridViewColumn Width="93">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ заказа" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding Orders}"
                                                         SelectedItem="{Binding SelectedOrder}"
                                                         DisplayMemberPath="Number"
                                                         Width="Auto"
                                                         MinWidth="50"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedOrder}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для D3 -->
                    <GridViewColumn Width="163">
                        <GridViewColumn.Header>
                            <TextBlock Text="Д3" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding D3s}"
                                                         SelectedItem="{Binding SelectedD3}"
                                                         DisplayMemberPath="Note"
                                                         Width="Auto"
                                                         MinWidth="50"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedD3}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для ПР -->
                    <GridViewColumn Width="40">
                        <GridViewColumn.Header>
                            <TextBlock Text="ПР" TextWrapping="Wrap"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding PR}" MinWidth="25"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для GM -->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ ГМ" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding GMNumbers}"
                                                         SelectedItem="{Binding SelectedGMNumber}"
                                                         DisplayMemberPath="NumberGM"
                                                         MinWidth="35"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedGM}"/>

                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для SPU -->
                    <GridViewColumn Width="130">
                        <GridViewColumn.Header>
                            <TextBlock Text="Обозначение СПУ" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding SelectedSPU.Note}"
                                               Width="Auto"
                                               MaxHeight="50"
                                               TextWrapping="Wrap"/>

                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Колонка для SPU -->
                    <GridViewColumn Width="170">
                        <GridViewColumn.Header>
                            <TextBlock Text="Наименование СПУ" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SelectedSPU.Name}"
                                           MaxHeight="50"
                                           TextWrapping="Wrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для  Manufactory-->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="№ Пр-ва" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding Manufactories}"
                                                         SelectedItem="{Binding SelectedManufactory}"
                                                         DisplayMemberPath="Number"
                                                         Width="Auto"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedManufactory}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для PlannedDeadline-->
                    <GridViewColumn Width="105">
                        <GridViewColumn.Header>
                            <TextBlock Text="Плановый срок сдачи ГМ" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBox Text="{Binding PlannedDeadline, Mode=OneWay}" Visibility="Collapsed"/>
                                    <DatePicker SelectedDate="{Binding PlannedDeadline}"
                                                DisplayDateStart="01/01/2025"
                                                DisplayDateEnd="01/01/2050"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <!-- Колонка для PlannedDeadlineWeek-->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="План-нед" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PlannedDeadlineWeek}" MinWidth="40"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для GMDNackaldania -->
                    <GridViewColumn Width="70">
                        <GridViewColumn.Header>
                            <TextBlock Text="Накладная" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Waybill}" MinWidth="50"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для NackadnaiaDate-->
                    <GridViewColumn Width="105">
                        <GridViewColumn.Header>
                            <TextBlock Text="Дата накладной" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBox Text="{Binding WaybillDate, Mode=OneWay}" Visibility="Collapsed"/>
                                    <DatePicker SelectedDate="{Binding WaybillDate}"
                                                DisplayDateStart="01/01/2025"
                                                DisplayDateEnd="01/01/2050"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для FactWeek-->
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="Факт_нед" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FactWeek}" MinWidth="30"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Причины задаржки сдачи ГМ-->
                    <GridViewColumn Width="130">
                        <GridViewColumn.Header>
                            <TextBlock Text="Причины задаржки сдачи ГМ" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding WhyDelay}"
                                         MaxHeight="50"
                                         TextWrapping="Wrap"
                                         MinWidth="50"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Колонка для Contragent-->
                    <GridViewColumn Width="120">
                        <GridViewColumn.Header>
                            <TextBlock Text="Контрагент" TextWrapping="Wrap" MaxHeight="50"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <local1:ComboTextBox ItemsSource="{Binding Contragents}"
                                                         SelectedItem="{Binding SelectedContragent}"
                                                         DisplayMemberPath="Name"
                                                         Width="Auto"/>
                                    <Grid Background="Red"
                                          Width="3"
                                          Visibility="{Binding IsSelectedContragent}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Style="{DynamicResource ButtonYesStyle}"
                    Command="{Binding AddGMCommand}"
                    Grid.Column="0"
                    Content="Добавить"/>
            <Button Style="{DynamicResource ButtonNoStyle}"
                    Command="{Binding DeleteGMCommand}"
                    Grid.Column="1"
                    Width="Auto"
                    Content="Удалить"/>
            <Button Style="{DynamicResource ButtonYesStyle}"
                    Command="{Binding SaveCommand}"
                    Grid.Column="2"
                    Width="Auto"
                    Content="Сохранить"/>
            <Button Style="{DynamicResource ButtonYesStyle}"
                    Command="{Binding LoadListGMCommand}"
                    Grid.Column="3"
                    Width="Auto"
                    Content="Загрузить список"/>
        </Grid>

    </Grid>
</Page>
