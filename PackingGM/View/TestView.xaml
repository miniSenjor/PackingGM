<Page x:Class="PackingGM.View.TestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PackingGM.View"
      xmlns:local1="clr-namespace:PackingGM.View.Control"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestView">

    <Grid Style="{DynamicResource GridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Command="{Binding BackCommand}"
                    Style="{DynamicResource ButtonYesStyle}"
                    Content="Назад"
                    Width="50"
                    HorizontalAlignment="Left"/>
            <TextBlock Text="Страница заполнения бд"
                       Style="{DynamicResource TextBlockHeadStyle}"
                       FontSize="20"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Таблица для заполнения" Style="{DynamicResource TextBlockStyle}"/>
            <TextBox Text="{Binding Query}"/>
            <Button Command="{Binding RunQueryCommand}"
                    Content="Выполнить"/>
            <TextBlock Text="{Binding Respons}"/>
        </StackPanel>
    </Grid>
</Page>
